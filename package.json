{
  "name": "campo-reply-parser",
  "version": "1.0.0",
  "description": "Webhook server: classifies insurance email replies as human or automated using Claude, then updates HubSpot deals.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
